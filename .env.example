# Optional: Set this only if you want /agent to use OpenAI. If empty, /agent falls back to echo.
OPENAI_API_KEY=

# Default Edge TTS voice (change to any Azure/Edge voice name you prefer)
DEFAULT_VOICE=en-NG-EzinneNeural

# Requests per minute per IP
RATE_LIMIT_PER_MIN=60
